id: citibike-elt
namespace: company.team
description: Extract, Transform and Load Citibike rides data

variables:
  data_month: "{{ trigger.date ?? execution.startDate | dateAdd(-1, 'MONTHS') | date('yyyyMM') }}"
  file_uri: "https://s3.amazonaws.com/tripdata/{{ vars.data_month }}-citibike-tripdata.zip"
  gcs_path: "gs://{{ kv('GCP_BUCKET_NAME') }}/{{ trigger.date ?? execution.startDate | dateAdd(-1, 'MONTHS') | date('yyyy-MM') }}"

tasks:
  # ---------------------------------------------------------
  # 0. GCP KV SETUP
  # ---------------------------------------------------------
  - id: setup_gcp_kv
    type: io.kestra.plugin.core.flow.Subflow
    namespace: company.team
    flowId: gcp-kv
    wait: true
    transmitFailed: true

  # ---------------------------------------------------------
  # 1. EXTRACT AND LOAD RAW FILES TO GCS
  # ---------------------------------------------------------
  - id: extract_raw_data
    type: io.kestra.plugin.core.flow.Subflow
    namespace: company.team
    flowId: data-extract
    inputs:
      http_uri: "{{ render(vars.file_uri) }}"
      gcs_path: "{{ render(vars.gcs_path) }}"
      data_month: "{{ render(vars.data_month) }}"
    wait: true
    transmitFailed: true

  # ---------------------------------------------------------
  # 2. GCS -> BIGQUERY RAW INGESTION
  # ---------------------------------------------------------
  - id: create_ext_trips_table
    type: io.kestra.plugin.gcp.bigquery.Query
    sql: |
      CREATE OR REPLACE EXTERNAL TABLE `{{ kv('GCP_PROJECT_ID' ) }}.{{ kv('GCP_DATASET_RAW') }}.trips_ext`
      (
        ride_id STRING OPTIONS (description = 'Ride ID'),
        rideable_type STRING OPTIONS (description = 'Rideable type'),
        started_at STRING OPTIONS (description = 'Started at'),
        ended_at STRING OPTIONS (description = 'Ended at'),
        start_station_name STRING OPTIONS (description = 'Start station name'),
        start_station_id STRING OPTIONS (description = 'Start station ID'),
        end_station_name STRING OPTIONS (description = 'End station name'),
        end_station_id STRING OPTIONS (description = 'End station ID'),
        start_lat STRING OPTIONS (description = 'Start latitude'),
        start_lng STRING OPTIONS (description = 'Start longitude'),
        end_lat STRING OPTIONS (description = 'End latitude'),
        end_lng STRING OPTIONS (description = 'End longitude'),
        member_casual STRING OPTIONS (description = 'Member or casual ride')
      )
      OPTIONS (
        format = 'CSV',
        uris = ['{{ render(vars.gcs_path) }}/{{ render(vars.data_month) }}-*.csv'],
        skip_leading_rows = 1,
        ignore_unknown_values = TRUE
      );

  - id: upload_raw_trips_table
    type: io.kestra.plugin.gcp.bigquery.Query
    destinationTable: "{{ kv('GCP_PROJECT_ID' ) }}.{{ kv('GCP_DATASET_RAW') }}.trips"
    writeDisposition: WRITE_APPEND
    sql: |
      SELECT
        TIMESTAMP('{{ execution.startDate }}') AS upload_datetime,
        *
      FROM `{{ kv('GCP_PROJECT_ID' ) }}.{{ kv('GCP_DATASET_RAW') }}.trips_ext`
    timePartitioningField: upload_datetime
      
#  # ---------------------------------------------------------
#  # 3. BIGQUERY DATA TRANSFORMATIONS
#  # ---------------------------------------------------------
#  - id: transform_raw
#    type: io.kestra.plugin.core.flow.Subflow
#    namespace: company.team
#    flowId: data-transform
#    wait: true
#    transmitFailed: true
#
  - id: purge_files
    type: io.kestra.plugin.core.storage.PurgeCurrentExecutionFiles
    description: To avoid cluttering your storage, we will remove the downloaded files

pluginDefaults:
  - type: io.kestra.plugin.gcp
    values:
      serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
      projectId: "{{ kv('GCP_PROJECT_ID') }}"
      location: "{{ kv('GCP_LOCATION') }}"